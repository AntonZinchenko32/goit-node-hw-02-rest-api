const {
  HOST_URL,
  PORT,
  DB_MONGO_HOST,
  DB_MONGO_USER,
  DB_MONGO_PASSWORD,
  DB_MONGO_DATABASE,
  SECRET,
  NODEMAILER_CONFIG_PASSWORD,
} = require("./env");
const { TEMP_UPLOAD_DIR, UPLOAD_DIR } = require("./common");

module.exports = {
  HOST_URL,
  PORT,
  DB_MONGO_HOST,
  DB_MONGO_USER,
  DB_MONGO_PASSWORD,
  DB_MONGO_DATABASE,
  SECRET,
  TEMP_UPLOAD_DIR,
  UPLOAD_DIR,
  NODEMAILER_CONFIG_PASSWORD,
};
